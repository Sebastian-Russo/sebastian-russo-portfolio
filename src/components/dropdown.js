import React from 'react'; 
import '../styling/dropdown.css';

export const Dropdown = () => {

  return (
    <select>
      <option>a</option>
    </select>
  )
}

// list all the projects with type from arg 